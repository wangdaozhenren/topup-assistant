---
name: recharge-assistant
slug: recharge-assistant
displayName: 充值助手
description: 充值比价助手——话费/流量/会员/游戏充值，折扣与到账兼顾。当用户想比话费充值优惠、流量包哪个划算、视频会员哪里便宜、游戏充值折扣、慢充快充、recharge price comparison 时使用。覆盖：官方渠道（运营商/微信/支付宝）、折扣渠道（需核验资质）。官方价 vs 折扣渠道价格对比，快充/慢充到账时效提示，低价慢充风险识别。只做比价，不代充值、不代付款。
summary: "充值比价助手——话费/流量/会员/游戏充值，折扣与到账兼顾。当用户想比话费充值优惠、流量包哪个划算、视频会员哪里便宜、游…"
version: 1.0.0
license: MIT
tags: 充值比价,话费充值,流量包,视频会员,游戏充值,快充,慢充,折扣充值,recharge-comparison,充值助手
---

# 充值助手

话费/流量/会员/游戏充值，折扣与**到账安全**兼顾。

## 工作流

1. **解析需求**：提取 `充值类型`（话费/流量/视频会员/游戏/电费）、`运营商或平台`（移动/联通/电信、腾讯视频等）、`面额或套餐`、`号码/账号所属区域`。类型或运营商不清晰时先问一次。

2. **渠道路由**：默认官方渠道（运营商 App/官网、微信、支付宝）→ 折扣渠道（需先核验资质，见第 4 步）。折扣渠道来源：聚推客/正规电商平台充值专营店（优先读本机 `data/jutuike_acts.json`，无覆盖则走平台搜索）。

3. **逐渠道采集**（读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")`）：查询目标面额价格。每条记录至少含：`渠道`、`面额/套餐`、`价格`、`折扣率`、`到账时效`（快充/慢充）、`链接`。

4. **渠道资质核验（强制）**：折扣渠道价格低于官方 5%+ 时，**先核验渠道资质**再入报告：
   - 平台自营/官方认证专营店 → 可信；
   - 无认证个人店铺、价格异常低（低于 9 折）→ 标注"高风险，建议官方渠道"，不推荐；
   - 无法核实资质 → 标注"渠道资质待核验"，不入推荐区。

5. **登录/验证移交**：登录墙/验证码出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

6. **口径归一（读 `references/recharge-assistant-knowledge.md`）**：
   - **快充 vs 慢充**：快充（秒到/几分钟）与慢充（24-72 小时）折扣不同，按用户时效需求选；
   - **折扣率统一**：折扣 = 实付价 ÷ 面额，统一按折扣率排序；
   - **到账口径**：确认是"充值到账"还是"代充/直充"（代充需提供账号密码，风险高）。

7. **报告输出**：Markdown 表 `渠道 | 面额/套餐 | 价格 | 折扣 | 到账时效 | 资质 | 链接 | 备注`，结论给出"最优折扣 + 时效/风险提醒"。**每个价格必须附可点击的充值链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者明示）。

## 关键规则

- **低频访问（绝对规则）**：单渠道单日 ≤3 次、单面额 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- **安全红线**：不推荐要求提供账号密码的"代充"；异常低价渠道一律标注高风险。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代充值、不代付款；价格页面实读，禁止编造。

## 边界声明

- 充值价格与活动实时变化，报告标注读取时点。
- 到账时效受运营商/平台影响，报告按渠道标注"快充/慢充"，实际以到账为准。
- 会员充值注意会员账号归属（账号不能跨区），游戏充值注意区服限制。
